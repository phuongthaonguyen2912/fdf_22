<div class="container-fluid">
  <div class="row">
    <div class="col-sm-3 col-md-2 sidebar">
      <div class="brand"><%= t("static_pages.home.brand") %></div>
      <%= render @supports.all_category %>
    </div>
    <div class="col-sm-9 col-sm-offset-3 col-md-10 col-md-offset-2 main">
      <div class="toolbar">
      <%= t(".category", category_name: @category.name) %></div>
      <%= render @supports.products_category %>
    </div>
  </div>
</div>

<div class="container-fluid">
  <div class="row">
    <div class="col-sm-3 col-md-2 sidebar">
      <div class="brand"><%= t("static_pages.home.brand") %></div>
      <%= render @categories %>
    </div>
    <div class="col-sm-9 col-sm-offset-3 col-md-10 col-md-offset-2 main">
      <div class="toolbar"><%= t("views.categories.show.category",
        params: @category.name) %></div>
      <%= render @products %>
    </div>
  </div>
</div>

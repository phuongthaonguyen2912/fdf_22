<%= link_to(t("layouts.cart_text.text",
  order_items_size: current_order.order_items.size,
  total_price: number_to_currency(current_order.subtotal)),
  cart_path(session[:order_id]), class: "btn btn-link") %>
